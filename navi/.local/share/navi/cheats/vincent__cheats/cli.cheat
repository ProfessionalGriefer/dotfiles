% CLI

# Remove all image metadata
exiftool -all= <FILE>

# Show all image metadata
exiftool <FILES>

# Mirror website 
wget -mpEk <URL>

# SSH Connection to Hetzner
ssh vincent@78.46.212.122

# Generate random string
openssl rand -base64 36

$ FILES : ls
